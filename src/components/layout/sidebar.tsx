// Deprecated: replaced by "@/components/layout/app-sidebar" (AppSidebar).
// Kept only because this environment cannot delete files from the mounted
// folder — safe to delete by hand.
export { AppSidebar as Sidebar } from "@/components/layout/app-sidebar";
