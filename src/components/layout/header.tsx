// Deprecated: replaced by "@/components/layout/app-header" (AppHeader).
// Kept only because this environment cannot delete files from the mounted
// folder — safe to delete by hand.
export { AppHeader as Header } from "@/components/layout/app-header";
