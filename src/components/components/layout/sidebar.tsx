// Deprecated duplicate location (this "components/components" nesting was a
// mistake from an earlier scaffold). The real implementation now lives at
// "@/components/layout/app-sidebar". This file could not be deleted because
// the mounted project folder in this environment does not allow unlinking
// files — it is safe to delete by hand from your file explorer.
export { AppSidebar as Sidebar } from "@/components/layout/app-sidebar";
